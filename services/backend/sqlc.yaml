version: '2'
sql:
  - engine: postgresql
    schema: migrations
    queries: queries
    gen:
      go:
        package: dbgen
        out: internal/dbgen
        sql_package: pgx/v5
        emit_json_tags: true
        omit_unused_structs: true
        overrides:
          - db_type: uuid
            go_type: string
